# Staging Backup and Restore Record

**Scope:** staging only
**Capture:** 2026-09-11
**Restore status:** PASS for the M0.1 isolated relational-data recovery drill

## Backup Record

| Item | Evidence |
|---|---|
| Protected directory | `/home/aiprod/backups/aiprod-staging-M0-20260911T163323Z` |
| Directory/artifact modes | 700 / 600 |
| Custom dump | `staging-database.custom.dump`, 2,800,366 bytes |
| Globals dump | `staging-globals.sql`, 7,291 bytes |
| Archive SHA-256 | `4b64a31556228e640f22893c00ac7861079767f8ddc3bb88147c3172141022b4` |
| Archive readability | PASS: 927 TOC entries |
| Capture duration | 2 seconds |
| Free disk before/after | 144,231,852 KB / 144,229,048 KB |
| Source changed | No |

The globals file remains outside Git. Never print or commit role attributes, password hashes, connection strings, tokens, or row data.

## Restore Drill

The target was always a disposable `supabase/postgres:17.6.1.136` container with no network or published ports, a fresh volume, PostgreSQL 17.6, bootstrap role `supabase_admin`, and `wal_level=logical`.

Required shared preload libraries are `pg_stat_statements`, `pgaudit`, `plpgsql`, `plpgsql_check`, `pg_cron`, `pg_net`, `pgsodium`, `auto_explain`, `pg_tle`, `plan_filter`, and `supabase_vault`. The session preload library is `supautils`.

All observed disposable containers and volumes were removed after attempts.

| Stage | Result | Finding |
|---|---|---|
| Image startup as `supabase_admin` | PASS | Ready in about 3 seconds |
| Image startup as `postgres` | FAIL/intermittent | Image bootstrap assumes `supabase_admin` |
| Globals replay | FAIL | Bootstrap roles already exist; source role attributes destabilized the target |
| Full schema replay | FAIL | Dump drops/recreates extensions; preloaded `pg_net` terminated the connection |
| Custom restore, minimal roles | FAIL | ACL required `supabase_functions_admin` |
| Custom restore, role-name stubs | FAIL | Disposable role bootstrap did not complete deterministically |
| Pinned platform and service migrations | PASS | Auth and Storage schemas were regenerated by their owning services |
| AIProd baseline migration | PASS | Application schema applied with `ON_ERROR_STOP=1` |
| Filtered data restore | PASS | 60 table-data and 4 sequence entries restored in one transaction |
| Schema and aggregate row parity | PASS | 29 public tables, 82 policies, and selected row counts matched |
| Auth relationships | PASS | Zero orphan profiles and zero orphan identities |

The earlier full-dump attempts failed because they mixed platform bootstrap, service migration ledgers, extension lifecycle, and application state. The supported recovery model now rebuilds those schemas from their pinned owners and restores only protected application data.

## Required Recovery Inputs

1. Pinned Compose, images, and database bootstrap files from `PLATFORM_BASELINE.md`.
2. Service-owned Auth and Storage migrations from the pinned service images.
3. AIProd migrations and required seed from the repository.
4. A verified custom archive and a reviewed data-only restore list for `public`, `auth`, and `storage`.
5. Separate protected handling for platform settings, secrets, Edge Functions, and Storage object bytes.
6. An isolated target with email, payment, push, cron, analytics, and OAuth effects disabled.

## Next Drill

1. Verify the archive SHA-256 and TOC.
2. Start the pinned platform with no external network and wait for final database initialization.
3. Let pinned Auth and Storage services complete their schema migrations.
4. Apply AIProd migrations and the repository seed.
5. Select data and sequence entries for `public`, `auth`, and `storage`, excluding regenerated `auth.schema_migrations` and `storage.migrations`.
6. Restore with `--data-only --exit-on-error --single-transaction --disable-triggers`.
7. Compare application fingerprints, aggregate row counts, Auth relationships, and Storage metadata counts, then destroy the target.

## Exit Criteria

- [x] Protected staging backup exists.
- [x] Checksum and TOC are readable.
- [x] Runtime prerequisites are recorded.
- [x] Supported filtered restore exits zero without ignored errors.
- [x] Schema, policy, grant, and aggregate row-count parity pass.
- [x] Auth relational integrity and Storage metadata counts pass.
- [ ] End-to-end Auth behavior with synthetic users is automated.
- [ ] Recovery objectives and artifact retention are approved.

Current staging has zero Storage buckets, zero Storage objects, and zero Vault secrets. Future Storage object bytes require a separate protected backup and restore path. Vault secrets and regenerated service migration ledgers are never restored from Git. No destructive staging or production operation is authorized by this record.